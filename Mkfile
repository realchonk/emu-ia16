.SUBDIRS: emu lib bin

include config.mk

EMU = $./emu/emu
DESTDIR ?= ${.OBJDIR}/$./_install
LIBDEPS = $./lib/crt0.o $./lib/user.x $./lib/libc/libc.a
.EXPORTS: EMU DESTDIR LIBDEPS

PREFIX ?= /
BINDIR ?= ${PREFIX}/bin
LIBDIR ?= ${PREFIX}/lib
INCDIR ?= ${PREFIX}/include
.EXPORTS: PREFIX BINDIR LIBDIR INCDIR

.asm.o:
	${AS} -felf32 -o $@ $<

.c.o:
	${CC} -c -o $@ $< ${CFLAGS}

include templates.mk
.expand dir
