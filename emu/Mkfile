BIN := emu

all: ${BIN}

install: all
	mkdir -p ${DESTDIR}
	cp -f ${BIN:F} ${DESTDIR}/

clean:
	rm -f ${BIN:F} *.core

emu: emu.c shim.S
	${HOSTCC} -o $@ emu.c shim.S -no-pie ${HOSTCFLAGS} ${HOSTLDFLAGS}
